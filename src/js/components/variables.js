const formInput = document.querySelector('.form-control');
const formLabel = document.querySelector('.feedback');
const form = document.querySelector('.rss-form');

export {
  formInput, formLabel, form,
};
